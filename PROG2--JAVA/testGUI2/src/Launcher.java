import javafx.application.Application;

public class Launcher {
    public static void main(String[] args) {
        // Questa riga dice a JavaFX di avviare la finestra definita in MainApp
        Application.launch(MainApp.class, args);
    }
}